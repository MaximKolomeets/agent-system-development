# NEXT_STEPS

1. Использовать `CODE_REVIEW_TASK_TEMPLATE.md` для первого безопасного review target implementation repository.
2. Проверять, что review tasks используют role-based branches, review-only scope и vendor-neutral report filenames.
3. Findings из review переводить в отдельные implementation PR только после решения пользователя.
4. Для новых target adoption tasks явно фиксировать lifecycle mode, selected branch model, наличие `developer` и запрет `fallback-to-main` для `standard developer workflow`.
