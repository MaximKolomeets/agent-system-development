# NEXT_STEPS

1. Проверить PR-2g.
2. Смержить PR-2g в `developer`.
3. Перенести `developer` в `main`.
4. Синхронизировать `developer` с `main`.
5. Повторить target repository dry run в `audit-only` режиме.
6. Проверить, что minimal first PR создает только `docs/agent-system/ADOPTION_AUDIT.md`.
7. Собрать Methodology feedback без private data.
